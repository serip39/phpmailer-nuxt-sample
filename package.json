{
  "name": "phpmailer-nuxt-sample",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "cross-env NODE_ENV=develop nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "copy": "rm -rf ./public_html/_nuxt && cp -r ./dist/ ./public_html/",
    "apache": "npm run build && npm run copy"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.11.0",
    "cross-env": "^7.0.2",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2"
  }
}
